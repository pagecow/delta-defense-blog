import {createMuiTheme} from "@material-ui/core/styles";

const theme = createMuiTheme({
    palette: {
        primary: {
            light: '#757575',
            main: '#616161',
            dark: '#424242'
        },
        secondary: {
            main: '#ffcd00',
        },
    },
})

export default theme;